export function CrystalSection() {
  return (
    <section id="section-5" className="h-screen flex flex-col items-center justify-center text-center px-6">
      <p className="reveal text-[10px] uppercase tracking-[0.3em] text-accent mb-4 font-mono">
        04 &mdash; Architecture
      </p>
      <h2 className="reveal reveal-delay-1 text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-5 font-heading">
        Engineered from <span className="text-accent">first principles</span>
      </h2>
      <p className="reveal reveal-delay-2 text-text-secondary max-w-lg text-base md:text-lg leading-relaxed">
        Every system we build starts with your core problem — not a template. Custom architecture. Custom agents. Custom results.
      </p>
    </section>
  )
}
